Table : users

Champs :
id (Clé primaire)
username
email
password
first_name
last_name

Table : devis

Champs :
id (Clé primaire)
user_id (Clé étrangère vers la table users)
title
description
status (par exemple, "En attente", "Approuvé", "Rejeté")


Table : roles

Champs :
id (Clé primaire)
name (par exemple, "Admin", "User")

Table : user_roles

Champs :
user_id (Clé étrangère vers la table users)
role_id (Clé étrangère vers la table roles)

Table : devis_history

Champs :
id (Clé primaire)
devis_id (Clé étrangère vers la table devis)
status (par exemple, "En attente", "Approuvé", "Rejeté")
timestamp


